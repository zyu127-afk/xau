# MT5 Guardian

`GoldTradingGuardian.mq5` is the final local execution/risk authority. It must compile and run on the actual MT5 installation before the system can leave demo acceptance. Python, AI and ATAS are never allowed to bypass its checks.
