# Versioning

`version.json` is the portable component/version manifest. Update component versions only after code and validation status change; do not use it to imply unperformed external-terminal validation.
