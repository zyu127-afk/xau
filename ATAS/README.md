# ATAS DataBridge

This directory contains the ATAS/Rithmic-side bridge. The repository can compile/test the SDK-independent core, but the final indicator/plugin binding must be built against the ATAS SDK installed on the user's Windows machine. The bridge is a sensor only; it never submits MT5 orders.
