# Tests

Repository CI validates deterministic Python logic and the SDK-independent .NET bridge core. External MT5/ATAS/Rithmic behavior is covered by `Docs/ACCEPTANCE.md` and must be executed on the target Windows machine.
