# Configuration

Copy `config.example.yaml` to `config.yaml` for local use. Copy `secrets.local.example` to `secrets.local` for the API key. `secrets.local` is intentionally gitignored and excluded from normal backups/releases.
